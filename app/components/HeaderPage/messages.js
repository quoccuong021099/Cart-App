/*
 * HeaderPage Messages
 *
 * This contains all the text for the HeaderPage component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.HeaderPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the HeaderPage component!',
  },
});
